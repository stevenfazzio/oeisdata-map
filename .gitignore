# Upstream sequence data (sparse-checkout via scripts/sync_seq.sh)
# `seq` matches both a directory and a symlink (which is what sync_seq.sh creates).
seq
.cache/

# Pipeline outputs
data/
logs/

# Python
__pycache__/
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# Env / OS
.env
.DS_Store
